description "electricbook server to handle shared editing of ebooks"
author "Craig Mason-Jones"

start on runlevel [2345]
stop on runlevel [016]

# Automatically restart if crashed
respawn

expect fork

# trouble with permissions, so we'll run as root
setuid electricbook
setgid electricbook

script
	cd /opt/electricbook/
	/opt/electricbook/bin/electricbook -logtostderr web &
end script

